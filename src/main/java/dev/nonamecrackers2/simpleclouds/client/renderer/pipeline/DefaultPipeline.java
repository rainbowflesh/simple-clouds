package dev.nonamecrackers2.simpleclouds.client.renderer.pipeline;

/**
 * Standard rendering pipeline used when no compat mod overrides are active.
 * All behaviour is inherited from {@link AbstractCloudsPipeline}.
 */
public class DefaultPipeline extends AbstractCloudsPipeline {
	protected DefaultPipeline() {
	}
}
